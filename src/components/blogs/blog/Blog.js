import React from 'react'
import styles from './blog.module.css'

export default function Blog({imgSrc,by,heading,isFirst}) {
  return (
    <div className={`${styles.Blog} ${(isFirst)&&styles.FirstBlog}`} >
        <div className={styles.ImgContainer}>
            <img src={imgSrc||"https://picsum.photos/500/400"}></img>
        </div>
        <p className={styles.AuthorNDate}>by: Admin | date</p>
        <h1>Where there is any mistake you have made it as a designer</h1>
        <p className={styles.Description}>Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.
        Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using
        
        Link is rendered outside of the Router context.
        Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.
        Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, 
        
        double-check that no part of your component tree using Link is rendered outside of the Router context.
        Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.
        Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.
        Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.
        Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.Once you ensure that the Link component is correctly used within a Router, the error should be resolved. If the problem persists, double-check that no part of your component tree using Link is rendered outside of the Router context.
        </p>
    </div>
  )
}
